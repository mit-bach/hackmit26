# Session 09 notes — Bot `ctl-cash`

Kernel status `HUMAN_REVIEW` stays fail-closed. Queue owner is this slug,
Profile `review-apply` for remittance and `review-rec` for bank-rec.

`ar-review-correct` remains an emergency CLI. It is not this Bot's completion
path. Planted unexplained difference stays unexplained. Concurrence
cannot produce `RECONCILED` or MATCHED over that residual. Helios-class
FEE_NETTED still requires Kernel fee evidence.

Read tools matching cash/apply are honest for review. Posting stays out of
this constructor.

Harness Handle files: `cash_recon.office.persist_harness_rec_queue`.
Live Pi completion was not proven in this slice.
