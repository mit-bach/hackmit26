# Session 06 notes — Bot apply

Constitution: `.cfo-v2/office/constitution.md`. SUPERSEDES human `ar-review-correct` as the completion path.

Session 01/02 facade exists under `office/computer/cfo/extensions/`. This session writes Kernel Handle files under `runs/ar/handles/`. Pi `bot_send_prompt` completion was not live-proven.

Session 09 owns `ctl-cash` / `review-apply`. Apply writes the packet and does not AUTO_APPLY past `HUMAN_REVIEW`.

Room `cash` already partitions apply, collect, cash, ctl-cash (4 members).
