# Bot `cash` proof

See `office/sessions/07-PROOF.md` for commands and Computer disk.

This Bot owns unmatched bank lines. Profiles `match` and `investigate` share the five Kernel read ops and do not union with `ctl-cash`. Seeded `$12.40` stays `HUMAN_REVIEW`. Fee journals stay unposted. Sign-off Handle is `ctl-cash` / `review-rec` with `humanQueue: false`.

Handle completion was not live-proven.
