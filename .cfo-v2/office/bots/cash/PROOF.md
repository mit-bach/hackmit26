# Bot `cash` proof

This Bot owns unmatched bank lines. Profiles `match` and `investigate` share the cash_recon reads plus `get_pipe_identifier` and do not union with `ctl-cash`. Seeded `$12.40` stays `HUMAN_REVIEW`. Fee journals stay unposted. Sign-off Handle is `ctl-cash` / `review-rec` as a Harness file with `humanQueue: false`. Trusted cash is `workspace/cash/trusted/<period>.json`. Close Handle only when Kernel allows.

Identifier trust: do not re-guess when apply/pay already named the counterparty. Missing identifier fails closed.

Handle completion on live Pi was not proven in this slice. Kernel / host proofs: `.cfo/tests/test_cash_office.py`.
