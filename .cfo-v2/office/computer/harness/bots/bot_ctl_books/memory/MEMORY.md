# ctl-books

Standing notes for this Bot. Not shared.

Refuse lock when `evaluate_close_gates` failed. Planted `$12.40` is unexplained, not timing. CONCUR does not produce CLOSED while gates fail.

Never edit `TXN-2026-09-015`. Never `create_accrual`.
