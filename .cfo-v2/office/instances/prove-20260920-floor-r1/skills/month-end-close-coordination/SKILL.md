---
name: month-end-close-coordination
description: Coordinates month-end close tasks from Kernel ready_tasks without inventing balances or marking CLOSED.
status: new
---

# Month-End Close Coordination

## Purpose

Profile `coordinate` has no Catalog ops. Copy Kernel `ready_tasks` and send the next treatment Wake. Arithmetic and close eligibility stay in Python.

## When to Use

Bot `close` wearing Profile `coordinate`. Not an office-live Catalog caller.

## Remainder

Choose the next Wake among Kernel-ready rows. Do not invent a second close DAG. Harbor-class vendor habit lives in Memory, not in this Skill.

Fail-closed Kernel statuses stay fail-closed. Queue owner is `ctl-books`. Coordinate does not mark CLOSED. `close.month_end` is the only lock door.

## Boundaries

- Do not invent ledger amounts or evidence.
- Do not override Python validation.
- Do not force-close a failed reconciliation.
- Do not ask a human. Do not mark CLOSED.
- Do not pretend this Profile calls Catalog ops.
