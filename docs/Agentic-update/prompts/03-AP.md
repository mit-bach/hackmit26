# Prompt 03 — AP (money out)

You are one implementing agent. You own **money out**: open bills (`ap`), the weekly payment-run draft (`pay`), and concurrence (`ctl-pay`). You do not pay. You do not execute ACH. You do not merge `ap` and `pay`. You do not collect AR. You do not close the month.

If you treat INV-S12 stub Handles as the judged story, judges see protocol on a marker, not payables.

Read first, in this order:

1. `docs/Agentic-update/prompts/00-SHARED-LAWS.md`
2. This file
3. `docs/Agentic-update/01-intended-office.md`
4. `docs/Agentic-update/02-failure-taxonomy.md`
5. `docs/Agentic-update/02b-inadequacy-index.md`
6. `docs/Agentic-update/03-novelty-boundary.md`
7. `docs/Agentic-update/04-what-is-good.md`
8. `docs/Agentic-update/pipes/README.md`
9. `docs/Agentic-update/pipes/ap.md`
10. `docs/Agentic-update/pipes/intake.md`
11. `docs/Agentic-update/surfaces/skills.md`
12. `docs/Agentic-update/surfaces/memory-and-learning.md`
13. `docs/Agentic-update/surfaces/grants-and-tools.md`
14. `docs/Agentic-update/surfaces/bot-md.md`
15. `docs/Agentic-update/surfaces/verifiers.md`
16. `docs/Agentic-update/evidence/live-computer-2026-09-20.md`
17. `design-workshop/dominik/cfo-bot-grain.md`
18. `design-workshop/dominik/cfo-office-processes.md`
19. `.cfo-v2/office/constitution.md`
20. `.cfo-v2/office/SUPERSEDES.md`
21. `.cfo-v2/office/bots/ap/BOT.md`
22. `.cfo-v2/office/bots/pay/BOT.md`
23. `.cfo-v2/office/bots/ctl-pay/BOT.md`
24. `.cfo-v2/office/bots/email/BOT.md`
25. `.cfo-v2/office/computer/skills/three-way-match-analysis/SKILL.md`
26. `.cfo-v2/office/computer/skills/ap-exception-investigation/SKILL.md`
27. `.cfo-v2/office/computer/skills/payment-prioritization/SKILL.md`
28. `.cfo-v2/office/computer/skills/early-payment-discount-evaluation/SKILL.md`
29. `.cfo/workflow.py`
30. `.cfo/agent.py`
31. `.cfo-v2/office/computer/cfo/catalog.overrides.json`
32. `.cfo-v2/office/computer/cfo/handle-map.json`
33. `.cfo-v2/office/computer/harness/roster.json`
34. `.cfo-v2/office/final-demo/CAPABILITIES.md` (AP learning and vendor bank-change are Not built — stay honest)

Then Read live Kernel and live Computer. Disk wins.

Do not read prompts 01, 02, 04, 05 except shared laws. If Floor outcomes 1–3 are not true: NOTES-floor-hole, continue on Kernel bills and Grants. Do not rewrite `RUN.md`.

Repo root: `/Users/dominikbach/olympus/hackmit/hackmit26`

---

## Mission

Process law: a vendor claims we owe them. We check we asked for it and we got it. We decide which approved bills get cash this week.

1. A vendor bill becomes an open bill with Kernel three-way facts.
2. `ap` proposes APPROVE or HOLD. It does not pay. It does not concur.
3. Approve-shaped drafts Handle `ctl-pay` / `review-match`. `must_hold` still wins.
4. Payable bills sit in the approved pool. `pay` drafts this week’s run from Kernel candidates. It does not execute ACH.
5. Every weekly draft Handles `ctl-pay` / `review-pay`. After concurrence, identified wires go to `cash` with `executed` false.
6. Unreceived work that belongs to the period Handles `close` as accrual input. That Handle is not approval.

Done when: every bill is matched or held, the run is a Kernel-netted list a Verifier can refuse, and cash has not left without concurrence.

Cartoon to avoid: “the AP agent pays the invoice.” Match and pay are different objects.

---

## Why this is a separate agent

Three-way match Kernel is already one of the strongest pieces in the repo. Mixing this with AR send or cash rec will either rewrite `must_hold` into a Skill again or treat six stub Handles as product.

You specialize in: a real Kernel invoice (not MSG-S12), pay-run on the Harness bus, next-period learning without editing `prior_cases.json` as the happy path, Verifier refuse-for-reason vs rubber stamp.

---

## Categories you close

- **T5** — pay-run never proven on the bus. Runner still host (T13).
- **T7** — skills and BOT.md restating `must_hold`.
- **T9** — Verifier re-performance vs denylist tension. Name it. Do not union `RECORD_TOOLS` onto `ctl-pay` if grain SoD forbids.
- **T10** — `prior_cases.json` static. `ap.self_improvement` NOT_IMPLEMENTED.
- **T11** — INV-S12 marker never in Kernel.
- **T12** — stub Handles as product.
- **T13** — `.cfo/workflow.py` still `run_agent` → `Runner.run_sync`.

---

## Objects you own

| Object | Owner | Dies when |
| --- | --- | --- |
| Open bill | `ap` | Paid, or disputed to zero |
| Match packet | `ap` writes; `ctl-pay` concurs | Concurrence + Kernel allow, or HOLD |
| Approved pool row | Kernel pool; `pay` reads | Paid or stripped |
| Payment-run draft | `pay` | `ctl-pay` release + Kernel net; still not a bank send |
| Identified outflow | handed to `cash` | Bank rec ticks it (04) |

Intake: Email or books Handle `ap` / `prepare` with a path whose `tools.get_invoice` returns found. A charge is not a bill. 02 owns remittance and dun. You own the bill id staying canonical. Dual intake must not mint two Kernel ids for one vendor PDF. If you touch Email missing-field, do not fork a second mailbox.

---

## Why the current AP fails

### Intake does not produce a Kernel bill (T11, T12)

Completed Handles used `MSG-S12` / `INV-S12`. Email packet was a marker. `tools.get_invoice(INV-S12)` not found. AP HOLD. ctl-pay REFUSE.

The bus moved. The pipe did not match a vendor bill.

Refuse-on-empty-evidence is **good**. Do not delete that behavior. Do not make INV-S12 the judged story. Land a real Maximor vendor bill (Acme `INV-001` class: `PO-101`, `GR-101`) as a Kernel invoice with canonical id.

`workspace/sources/email/MSG-S12.json` is a marker. Kernel has no INV-S12. Leave history. Stop repeating it as the product.

### Host still Runner (T13)

`run_ap_workflow` in `.cfo/workflow.py` still imports `run_agent`. Constitution voided Runner as the Bot bus. AP pytest is not office-live match.

You do not restore Runner as the office. Kernel constructors remain Grant source. Domain work the office claims as **office-live** must complete on Harness Handles. The Kernel function may still exist for tests. It must not be what RUN.md tells a judge to run as the office.

### BOT.md / cwd (T6)

`ap` and `pay` instructions point at `office/bots/.../BOT.md` from Computer cwd. Path missing. Floor owns layout. You own wording: standing identity, must-not, done-when. Strip `must_hold` case lists and INV-009-class case law from standing identity. Pay BOT.md names INV-009-class unnecessary early pays. That is a planted demo id leaking into identity.

### No AP learning (T10)

`prior_cases.json` is a static seed. `run_ap_workflow` does not append. Demo gap `ap.self_improvement` is `NOT_IMPLEMENTED`. The next similar bill does not change. Maximor asked for memory that changes the next period.

A stored alias (INV-021 class) can apply only when current evidence still supports it. Precedent cannot override a live blocking `must_hold`. How you store habit is yours. The capability must exist. Do not claim the gap is closed by a Skill paragraph. Do not make a human editing `prior_cases.json` the happy path.

### Vendor bank-change control missing (T3)

Vendor master may now have bank fields. There is no control engine. Planting “wire instructions changed” as an audit find would be fake. Capabilities.md records this. **Keep that honesty.** Do not implement a fake control to look complete. Do not add a Bot for it.

### Pay-run never proven on the bus (T5)

Routine `weekly-pay-run` is on the Roster. `client.json` has `autoRoutines: false` (Floor). No live Handle `pay` → `ctl-pay` / `review-pay` is in the 09-20 Handle set (those six files are email/ap/ctl-pay stub match, not a weekly run).

After `ctl-pay` concurs on match and Kernel still allows, the id sits in the approved pool. Then `pay` / `schedule` drafts. Kernel `apply_cash_and_policy_net` binds amounts. The model does not breach the reserve. Draft Handles `ctl-pay` / `review-pay`. After concurrence, identified outflows Handle `cash` with `executed` false.

You must show a payment-run Handle to `ctl-pay`, or honest NOTES why not (blocked on Floor autoRoutines / no live Pi). Kernel-only `apply_cash_and_policy_net` pytest is not this outcome.

### No send-as-bank (intended gap)

Pay BOT.md: this office has no send-as-bank Connector. Do not add one for the demo. Identified wires must still reach `cash`. That handoff is yours to emit. 04 ticks the bank. You do not implement cash rec.

### Verifier cannot re-perform the bill (T9)

`ctl-pay` / `review-match` denylist strips `RECORD_TOOLS`. It may call `get_case_evidence`. It must not call `get_invoice` if SoD forbids. A Verifier that can only read the preparer’s packet can rubber-stamp or refuse for incompleteness. It cannot always re-perform three-way match from source. Live INV-S12 refuse was incompleteness. That is weaker than re-performance.

Name the tension. Do not union `RECORD_TOOLS` onto `ctl-pay` if grain SoD forbids that Grant union. If re-performance needs a **read**, that is a Grant question, not a new Bot. `ctl-pay` looks for reasons to refuse. Kernel still vetoes. A Verifier that concurs on a planted trap Kernel marked illegal is a bug in the Verifier.

`ctl-pay` / `review-pay` may only read cash position and treasury policies after denylist. It cannot see the candidate list it was denylisted from rebuilding. Refuse-for-incompleteness is easy. Refuse-for-wrong-rank is hard without facts. Do not give Payment Audit the same ranking Skill as a second doer if that makes SoD mush. You may split remainder without adding a Bot.

### Skills restate Kernel (T6, T7)

`three-way-match-analysis` Procedure: copy exception_types; clean match is approved PO + exact amount + exact vendor + full receipt + no duplicate. That is `must_hold` / `collect_case_evidence` in English.

`ap-exception-investigation` should be about how this vendor’s mess differs from the type list. Not a second taxonomy.

`payment-prioritization` and `early-payment-discount-evaluation` are shared by Payment Scheduler and Payment Audit. Soft SoD hole even when rebuild ops are denylisted.

Do not freeze `exception_type` → English table. Kernel already typed the exception. The specialist adds vendor-specific sense.

BOT.md tells when to wear Profile `investigate`. Kernel `exception_types` already exists. Do not add `ap-investigator` as a Bot. Same open item. Profile `investigate`.

### HOLD is a graveyard unless exceptions get worked

AP has no Kernel `HUMAN_REVIEW` path (`APPROVE`/`HOLD` only). That is good. Uncertainty is HOLD or investigate, then `ctl-pay`. The inadequacy: HOLD is a graveyard unless someone works exceptions. There is no proven loop that asks World for a missing PO or a revised PDF and then re-wakes `ap` on the same `invoice_id`. 02 owns World/send. You may Handle Email for missing field. You must not silently approve the original bill when the reply is missing.

No-PO bills, in process law, replace the PO leg with approval. Grain puts that approval on `ctl-pay`, not a human. Live office has not shown a no-PO bill through that gate. If you show one, Kernel still wins on amount/vendor facts you do have.

### Unreceived period work

Handles `close`. That Handle is not approval. 05 owns treatments. You emit the input.

---

## What is good (do not delete)

- Split `ap` vs `pay` vs `ctl-pay` matches process law and grain Test C.
- Kernel `collect_case_evidence` + `must_hold`: duplicate invoice number, missing PO, missing GR, amount past P-009, unknown vendor, PO over authority.
- APPROVE only enters the pool. HOLD does not become a forecast commitment or a clean close item.
- Payment Kernel `apply_cash_and_policy_net` strips HOLD, strips unnecessary early pays, defers for reserve.
- Live Pi: `ctl-pay` REFUSED an incomplete APPROVE packet for INV-S12. Verifier found reasons to refuse. Keep that instinct on real packets.
- Grant denylist stops Payment Audit from rebuilding the plan. Shape is right.
- Skills as a layer: the idea is right even when the files restate Kernel.

---

## Files you may create

- A real intake path that lands a Kernel invoice (test fixture or simulated mailbox message that `get_invoice` finds). Not MSG-S12.
- AP operational memory that a later similar bill can retrieve. Not a human-edited `prior_cases.json` as the happy path.
- Tests: `get_invoice` finds the id Email landed; HOLD invoice absent from `pay_this_week`; payment-run Handle shape to `ctl-pay`.
- `office/bots/ap/NOTES.md`, `pay/NOTES.md`, or `docs/Agentic-update/evidence/NOTES-ap.md`.

## Files you may edit

- `.cfo/workflow.py`, `.cfo/agent.py`, AP scheduling / pay-run Kernel modules, AP tests
- Invoice ingestion only as needed so Email lands a canonical id. Do not fork a second mailbox (02)
- Live Grants via compiler: AP Agent, Payment Scheduler, Payment Audit / ctl-pay twins. Respect denylist.
- `catalog.overrides.json` denylist — tighten or add a **read** for re-performance only if SoD still holds. Do not give write/rebuild to the Verifier
- `handle-map.json` AP/pay/ctl-pay edges. Unreceived → close. Identified outflow → cash
- `roster.json` ap / pay / ctl-pay connectors and Routine `weekly-pay-run` prompt text. Floor owns autoRoutines flag
- `.cfo-v2/office/bots/ap/BOT.md`, `pay/BOT.md`, `ctl-pay/BOT.md`
- The four AP skills listed in Read first. Kernel copies if compiler source
- Email BOT.md only for bill-dispatch handoff sentences, not World bind (02)

## Files you must not edit

- `.cfo/inbox/tools.py` send (02), `.cfo/ar/`
- `.cfo/cash_recon/`, `.cfo/close/` except you may call close Handle for unreceived
- `RUN.md`, intercept default (Floor)
- `collect` / `apply` / `world` / `cash` / `close` / `story` / `audit` BOT.md
- `examples/cfo-floor`, `web/`, instance snapshots
- Stripe Grants (04)

---

## Outcomes on open items

1. A real Maximor vendor bill lands as a Kernel invoice (canonical id), not `workspace/sources/email/MSG-S12.json` as a marker. Email or books Handle `ap` / `prepare` with a path whose `tools.get_invoice` returns found.
2. Three-way match: Kernel `collect_case_evidence` and `must_hold` still win. APPROVE-shaped packets Handle `ctl-pay` / `review-match`. HOLD does not enter the pay pool. Duplicate, missing PO, missing GR, amount past policy stay HOLD.
3. After ctl-pay concurs and Kernel still allows, the id sits in the approved pool. Routine `weekly-pay-run` (or a live Handle) wakes `pay` / `schedule`. Kernel `apply_cash_and_policy_net` binds amounts. The model does not breach the reserve. Draft Handles `ctl-pay` / `review-pay`. After concurrence, identified outflows Handle `cash` with `executed` false.
4. Unreceived period work Handles `close`. That Handle is not approval.
5. Next-period AP behavior can change without a human editing `prior_cases.json` as the happy path. Precedent cannot override a live blocking `must_hold`.
6. Skills and BOT.md: standing identity and must-not stay. Do not ship a second `must_hold` checklist as the specialist. ctl-pay looks for reasons to refuse.
7. Do not implement send-as-bank. Do not add `ap-investigator` as a Bot. Profile `investigate` stays on `ap`.
8. Do not restore Runner as the office bus. Kernel constructors remain Grant source.

---

## Novelty fence

You invent how `ap` notices this vendor’s messy PDF, how `pay` defers when cash is tight using Kernel candidates, Memory schema.

Do not:

- Merge `ap` and `pay`
- Make `ap` release cash
- Make `ctl-pay` hold `RECORD_TOOLS` just to feel powerful if grain SoD forbids
- Add `ap-investigator` as a sixteenth Bot
- Implement send-as-bank for the judged demo
- Freeze exception_type → HOLD sentence
- Invent amounts
- Claim vendor bank-change control is built
- Resolve $12.40 inside AP

---

## Proof / acceptance

1. One live or tested path where `get_invoice` finds the id Email (or books) landed. Canonical Maximor id, not INV-S12.
2. `ctl-pay` sees Kernel evidence on that packet (or refuses for a named reason).
3. HOLD invoice absent from `pay_this_week`.
4. A payment-run Handle to `ctl-pay` exists, or honest NOTES why not (Floor hole named).
5. INV-S12 stub is not the judged story. Refuse-on-marker still works if that fixture remains.
6. Show AP memory retrieval on a second similar bill **or** NOTES that learning is still NOT_IMPLEMENTED (do not lie in CAPABILITIES.md).
7. Do not claim office-live from `test_workflow.py` `INV-001` Kernel host alone.

When you finish, list files changed and the invoice id that `get_invoice` finds.

---

## Stop conditions

- If you cannot land a real bill without live Gmail, use simulated mailbox / World pack documents. Simulated transport is the product. Live Gmail is a lie.
- If Floor has not unified Handle stores, still emit Harness Handles. Do not invent a third CONCUR file.
- If 02 has not sent, you may still Handle Email for missing PO. You may not require World if it is unbound. NOTES.
- If denylist vs re-performance cannot be resolved without SoD mush, keep denylist, refuse incompleteness, document the tension.

## Out of scope

AR send. World bind. Cash tick. Close lock. `--fake` runbook. Website. Commits unless the operator asks.
