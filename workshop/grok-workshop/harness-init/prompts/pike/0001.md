# seq 0001 | tag drive | id pike | spawn | 2026-09-19T19:04:27Z

I want you to put a tremendous amount of work and effort into this.

Read `/Users/dominikbach/.grok/skills/eng-orch/shared/personas/engineer.md` before other work. That file defines who you are and how you work.

You are pike. This window is a spawn. This turn is not a bid. There are no peers. This is a single job. Refine the part. Expect related work later on this id.

Workspace: `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init/engineers/pike/`

Directory you may write:
- `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init/engineers/pike/`
- Runtime state under `/Users/dominikbach/olympus/hackmit/hackmit26/Harness` that the test needs (roster JSON, rooms, session files, `.pi/`). You may create bots and rooms there.

Do not edit `extensions/*.ts`. Do not rewrite the floor protocol. Do not design a CFO office. Do not write at desk root. Do not create `Deployment/`. Do not write in `Deployment/`. Do not edit `RUNNING.md`, `KEPT.md`, `CAST.md`, `cast/`, `prompts/`, `AGENT-LOG.md`, `OPERATOR.md`, or Operator-Workspace. Do not spawn Grok children. Do not browse for skills.

Desk: `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init`

## Operator prompt file

Read this file next. Do not wait for a paste. It is the operator's intent. The latest operator prompt file wins on conflict.

`/Users/dominikbach/olympus/hackmit/hackmit26/Operator-workspace/Prompts/Harness analysis.md`

Also read `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init/OPERATOR.md`. That file is the shared rule that this path is required for every child.

## Job

Drive the live Foundry Pi harness until you can say, from the TUI and from disk, whether bots can communicate, where chat logs go, and what each Foundry extension actually does.

The operator is not happy with the extensions. They called them lightweight, lazy, and possibly non-functional. Your job is to observe that, not to agree in advance. Prove each claim with a command you ran, a pane you captured, or a file you opened.

This turn is live operations plus a report. It is not a redesign.

## Start: TypeScript

Before you open cmux, read these files in this order. Open each file. Do not grep them as a substitute for a full read.

1. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/extensions/lib/manifest.ts`
2. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/extensions/foundry-roster.ts`
3. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/extensions/foundry-comms.ts`
4. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/extensions/foundry-subagents.ts`
5. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/extensions/foundry-memory.ts`
6. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/extensions/foundry-routines.ts`
7. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/extensions/foundry-approvals.ts`
8. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/skills/foundry/SKILL.md`
9. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/package.json`
10. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/SOURCE.md`
11. `/Users/dominikbach/olympus/hackmit/hackmit26/Harness/AGENTS.md`

From those files, write down what each extension claims to provide: tools, slash commands, files it reads or writes, and how bots are supposed to talk. That list is the script for the live test. If a claim has no live probe, you did not finish.

## Host skill: cmux drive

Who: pike.

Read: `/Users/dominikbach/.grok/skills/cmux-harness-driver/SKILL.md`

When: immediately before you spawn or drive a cmux pane. Read it again before teardown.

Why: pass or fail is what the TUI shows. Source reads are supporting evidence only.

How: follow that skill's protocol and safety. Spawn an isolated workspace. Use `capture_pane` for layout. Drive with keys and slash-commands. For this turn the operator also authorized real bot tasks and bot-to-bot communication, so you may submit prompts inside the Pi TUI after the harness is up. Do not prompt the operator's Grok session. Do not use `cmux_launch_agents`. Do not drive `workspace:38`. That pane is the operator's Grok parent.

Search MCP schemas for cmux tools before you call them. Do not invent parameters.

## Live harness

Launch directory: `/Users/dominikbach/olympus/hackmit/hackmit26/Harness`

Pi is `/opt/homebrew/bin/pi` version `0.85.1`. The Foundry package is installed from that clone (`pi list` shows the Harness path).

A leftover verify workspace from the parent may still exist: `workspace:39` / `surface:40`, sidebar name `verify-foundry-pi`. Do not treat that as the operator's live Grok. You may spawn a fresh isolated Pi instead of hijacking it. If you reuse it, say so and still follow capture_pane plus teardown rules for anything you create.

Parent already observed:

- Foundry extensions load.
- `/foundry-roster` printed `Aegis: atropos, clio, hermes, hephaestus`.
- `/subagents` listed those four as `[runtime]`.
- `/room` opened an empty Agent Room overlay.
- Default model `openai-codex/gpt-5.4-mini` failed a turn: `Codex error: The 'gpt-5.4-mini' model is not supported when using Codex with a ChatGPT account.`

Re-verify. Do not cite the parent as proof.

If the default model still fails, change the model in the Pi TUI until a turn can complete, then continue the communication test. If no model works, write that block with quoted pane text. Do not stop at the first model error and call comms untested.

## Operations you must actually run

Do many operations. Thin slash-command tourism is a failure of this job.

1. Create or register distinct bots (or a new roster) so more than one named agent exists with different tasks. Do not treat "Aegis already listed" as the end of creation. Try the harness's own create/register path. If that path is missing or no-ops, that is a finding. Quote it.
2. Put at least two bots on different tasks. Submit real work into the TUI. Watch what happens.
3. Test bot-to-bot communication: `bot_send_prompt`, `bot_await_turn`, room send, or whatever the TypeScript actually registered. Record accept vs complete. Record whether a handle is a result.
4. Find where chat logs, session files, room logs, and memory files land on disk. Open those paths. Quote paths and a sample of what is in them. Search `~/.pi`, Harness, and any path the TypeScript names.
5. Exercise each Foundry extension as a live object:
   - roster
   - comms / handles
   - subagents / runtime agents
   - memory
   - routines
   - approvals / `ask_user`
   - rooms
6. For each extension, say what the TUI did, what disk changed, and what did nothing. A no-op is a result.
7. Do not select a destructive final commit. Escape out of create-room or spawn overlays unless the test requires the side effect. When the test requires a room or a bot, take the side effect and record it.

Capture the pane after each meaningful step. Layout claims need `capture_pane`, not `read_screen` alone.

## Product files that must exist when you stop

1. `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init/engineers/pike/DRIVE-REPORT.md`
2. `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init/engineers/pike/STOP.md`

`DRIVE-REPORT.md` must contain:

- What each TypeScript file claimed
- What you drove, in order
- workspace and surface refs you created
- quoted `capture_pane` findings
- whether bots communicated
- exact disk paths for chat logs, sessions, rooms, memory
- per-extension: works / no-op / error, with evidence
- what you did not prove

`STOP.md` must say done or blocked, which product files exist, and the next operator-visible fact.

## Host skill: STE

Who: pike.

Read: `/Users/dominikbach/.grok/skills/ste-writing/SKILL.md`

When: immediately before you write `DRIVE-REPORT.md` and immediately before you write `STOP.md`.

Why: lark and a later parent must act from these files after this window is gone.

How: aircraft-strict for steps, commands, and refuse lists. STE-flavored for judgment about what an extension actually did. Leave paths, command output, and package names intact. One name for one thing.

## Stop rule

Stop when both product files exist and the report answers, with evidence: did bots communicate, where do logs go, and what did each Foundry extension do live.

Stop if cmux is down or no model can complete a turn. Write the block. Still report everything you did prove.

Do not rewrite extensions. Do not write the analysis-hate document. That document is lark's job.

## Teardown

Close workspaces you created, or say why you left one open. Restore focus to `workspace:38` if you changed it. Do not close `workspace:38`.
