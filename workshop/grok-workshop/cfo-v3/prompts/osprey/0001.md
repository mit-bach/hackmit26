# seq 0001 | tag engineer | id osprey | spawn | 2026-09-24T21:46:00Z

Read `/Users/dominikbach/.grok/skills/eng-orch/shared/personas/engineer.md` before you act.

Desk: `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/grok-workshop/cfo-v3`. This is not a bid.

Write scope: `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/cfo/extensions/intercept.ts`, `call.ts`, `index.ts`, a new test file `extensions/verifier-args.test.ts`, and `engineers/osprey/`. Do not edit `package.json` or `node_modules` (another child is installing them). Do not edit `.harness`, `.cfo`, or `.cfo-v2`.

## Part

A verifier concurrence is matched by Bot, op, and idempotency key. It is not matched by the call's arguments. A Bot can get CONCUR on one accrual, then reuse the same key with a different amount, and the second call passes the verifier check.

Read the notes in `engineers/wren/PROVED.md` for how the lookup works now. Do not undo that work.

Store a hash of the canonical arguments on the pending verifier record when it is created. On unlock, require that the call's argument hash matches. A mismatch returns `verifier_required` and creates a new pending record. Existing records without a hash are treated as mismatched.

Add a test through the registered Pi tool path, as `verifier-wiring.test.ts` does: CONCUR on args A, then a call with args B and the same key stays locked.

## Done when

`engineers/osprey/PROVED.md` has the test command and output. The new test fails on the current code and passes on your change. `verifier-wiring.test.ts` still passes.
