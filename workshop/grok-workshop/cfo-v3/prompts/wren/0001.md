# seq 0001 | tag engineer | id wren | spawn | 2026-09-24T21:42:00Z

Read `/Users/dominikbach/.grok/skills/eng-orch/shared/personas/engineer.md` before you act.

Desk: `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/grok-workshop/cfo-v3`. This is not a bid.

Write scope: `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/cfo/extensions/` and `engineers/wren/`. Do not edit `.harness`, `.cfo-v3/kernel`, `.cfo-v3/office`, `.cfo-v3/skills`, JSON under `.cfo-v3/cfo/`, `.cfo`, or `.cfo-v2`.

## Part

Two verified defects from `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/docs/Office-show/DESIGN-REVIEW.md` section 3 live in this directory:

1. `index.ts` line ~191 calls `callConnectedTool(...)` with the verifier handle id `null`. A consequential op therefore returns `verifier_required` forever. Pass the real verifier handle id. Look up the pending verifier handle by idempotency key if the model does not pass one.
2. `intercept.ts` `handleConcurrence` treats any first line starting with `CONCUR`, or any text containing the word CONCUR without REFUSE, as CONCUR. `I do not concur` and `CONCUR is not possible` must return not-CONCUR. Accept only a structured decision: a first line exactly `CONCUR` or `REFUSE`, or a JSON `{"decision": ...}`. Anything else is not CONCUR.

Add tests beside the existing `facade.test.ts`. One test must go through the Pi tool registration path in `index.ts`, not call `callConnectedTool` directly. Run only those tests.

## Done when

`engineers/wren/PROVED.md` has the test command, its output, and the diff summary. The new tests fail on the old code and pass on the new code.
