# seq 0001 | tag writer | id lark | spawn | 2026-09-24T21:40:00Z

Read `/Users/dominikbach/.grok/skills/eng-orch/shared/personas/engineer.md` before you act. Read `/Users/dominikbach/.grok/skills/ste-writing/SKILL.md` and apply it to prose you edit.

Desk: `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/grok-workshop/cfo-v3`. This is not a bid.

Write scope: only markdown under `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/office/` and `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/skills/`. Notes go in `engineers/lark/`. Do not edit JSON, Python, TypeScript, `.cfo`, `.cfo-v2`, or `.harness`.

## Part

These files are the system prompt. `.harness/Harness-v2/src/context.ts` pastes `office/system.md`, each `BOT.md`, the active profile, and the skill bodies. Read `context.ts` once so you know what is pasted.

The outside review found the prompt is mostly prohibitions. Read sections 5 and 10 of `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/docs/Office-show/DESIGN-REVIEW.md`, specifically: 859 not/never/do-not lines, `## Must not` items that name actions the Bot has no tool for, "A peer Handle is not approval" repeated 20 times, and the same protocol restated in `system.md`.

Cut:

- Every `Must not` line whose action has no tool on that Bot. Check the Bot's grants in `.cfo-v3/cfo/grants.json` and `slug-map.json` before you cut. Keep the line if the Bot can do the thing.
- Repeated protocol text in `BOT.md` and profiles that `office/system.md` already says. Keep it once, in `system.md`.
- Sentences about how the office was built (sessions, Runner, Test A–D, constitution).

Keep: what the Bot owns, whom it hands work to, the Kernel facts it must not override, and `$12.40` stays unexplained.

Do not add Catalog op ids. Do not add sentences telling the Bot to read another file.

## Done when

`engineers/lark/COUNT.md` lists, for `office/` plus `skills/`, the count of `not`/`never`/`do not` and total words before and after, and every file changed.
