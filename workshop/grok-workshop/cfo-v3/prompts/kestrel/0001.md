# seq 0001 | tag engineer | id kestrel | spawn | 2026-09-24T21:40:00Z

Read `/Users/dominikbach/.grok/skills/eng-orch/shared/personas/engineer.md` before you act.

Desk: `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/grok-workshop/cfo-v3`. This is not a bid. You own one part.

Workspace you may write for notes: `engineers/kestrel/`. Product may also change `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/cfo/catalog.json` and `grants.json` and `slug-map.json` only. Do not write `.cfo-v3/kernel`, `.cfo-v3/office`, `.cfo-v3/skills`, `.harness`, `.cfo`, or `.cfo-v2`. Another child owns the kernel copy and is still running. Do not edit its files.

## Part

The Computer at `.cfo-v3` has a `catalog.json` and `grants.json` that were copied from `.cfo-v2`. They must be produced by the compiler against `.cfo-v3`, not copied.

Read `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/grok-workshop/cfo-v3/kernel-migration.md` and `office-layout.md`.

Find how the compiler is invoked. `.cfo-v2/office/RUN.md` names `PYTHONPATH=.cfo-v2/office python3 -m compiler --phase operational`. In v3 the compiler lives under `.cfo-v3/kernel/compiler`. Use `.cfo-v3/kernel/.venv/bin/python` and a `PYTHONPATH` that contains only `.cfo-v3/kernel`. Point its output at `.cfo-v3/cfo`.

If the compiler still reads constructors or paths from `.cfo` or `.cfo-v2`, do not patch the kernel. Write the exact import or path it reads into `engineers/kestrel/BLOCKED.md` and stop.

## Done when

- `engineers/kestrel/COMPILE.md` has the command, its exit status, the op count, and a diff summary against the copied files.
- If the compile ran, `.cfo-v3/cfo/grants.json` omits `audit.tools.get_audit_ground_truth` from operational grants.
- Or `engineers/kestrel/BLOCKED.md` exists and names the line that blocks it.
