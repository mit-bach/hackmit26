# seq 0001 | tag engineer | id sparrow | spawn | 2026-09-24T21:46:00Z

Read `/Users/dominikbach/.grok/skills/eng-orch/shared/personas/engineer.md` before you act.

Desk: `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/grok-workshop/cfo-v3`. This is not a bid.

Write scope: `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/cfo/package.json`, `package-lock.json`, `node_modules/`, `tsconfig.json`, and `engineers/sparrow/`. Do not edit `.cfo-v3/cfo/extensions/*.ts`. Another child just changed those. Do not edit `.harness`, `.cfo`, or `.cfo-v2`.

## Part

`.cfo-v3/cfo` has `package.json` and no `node_modules`. The extension imports `typebox`. A test just ran against a stub for `typebox`, and `tsc` did not run.

Install the dependencies the extension actually imports, and nothing else. Compare `.cfo-v3/cfo/package.json` to the imports in `.cfo-v3/cfo/extensions/*.ts`. Drop a dependency no file imports. If `tsconfig.json` is missing, copy the one from `.cfo-v2/office/computer/cfo/` and point it at `extensions/`.

Then run the type check and the existing tests from `.cfo-v3/cfo`:

- `npx tsc --noEmit -p tsconfig.json`
- `node --test extensions/verifier-wiring.test.ts`

If `tsc` reports errors in `extensions/*.ts`, do not fix them. Write each error into your report.

## Done when

`engineers/sparrow/INSTALL.md` has the dependency list, the tsc output, and the test output with `typebox` installed, not stubbed.
