# seq 0002 | tag engineer | id finch | resume | 2026-09-24T21:48:00Z

Same desk, same write scope as `prompts/finch/0001.md`: Python files under `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/kernel/` and `engineers/finch/`. Do not write `.cfo-v3/cfo`, `.cfo-v3/skills`, `.harness`, `.cfo`, or `.cfo-v2`.

## Part

Keep doing what you did in 0001: remove the dependency on skill loading, do not recreate it.

You reported eleven kernel files that import `skills.loader`. With those imports the sidecar fails with `No module named 'skills.loader'` when it attaches a Computer.

For each import: if it feeds a trace field, pass `None` or drop the field where the model allows it. If a function body needs skill text to run, return the same value the old loader returned with no skills loaded. Do not add `skills/loader.py`.

Then prove the sidecar serves:

1. Attach the real Computer, `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3`, with `PYTHONPATH` set only to `.cfo-v3/kernel`.
2. Start the sidecar on a free port. Do not use 8800, 8801, or any port already listening.
3. Call `kernel.health` over its RPC, then one granted read: `tools.get_invoice` for `INV-001`.
4. Stop the process you started.

## Done when

`grep -rn "skills.loader\|from skills" .cfo-v3/kernel --include=*.py` prints nothing.

`engineers/finch/SERVE.md` has the start command, the health response, the `INV-001` response (amount 12450), and the stop.
