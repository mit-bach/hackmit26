# seq 0001 | tag engineer | id finch | spawn | 2026-09-24T21:42:00Z

Read `/Users/dominikbach/.grok/skills/eng-orch/shared/personas/engineer.md` before you act.

Desk: `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/grok-workshop/cfo-v3`. This is not a bid.

Write scope: `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/kernel/` Python files and `engineers/finch/`. Do not edit `.cfo-v3/skills/*/SKILL.md`, `.cfo-v3/office`, `.cfo-v3/cfo`, `.harness`, `.cfo`, or `.cfo-v2`.

## Part

Fourteen kernel modules do `from skills import compose_instructions, skills_for` (`agent.py`, `reporting/agents.py`, `close/agents.py`, `cash_recon/agent.py`, `integrations/agent.py`, `scheduling/agent.py`, `ar/agents.py`, `inbox/agents.py`, `accrual/agent.py`, `prepaid/agent.py`, `bs_recon/agent.py`, `fixed_assets/agent.py`, `audit/agent.py`, `invoice_ingestion/agents.py`). The v3 copy has no `skills/__init__.py`, so those imports fail.

Those modules build old in-process `Agent(...)` objects. The office runs Bots through Harness and Pi, not through these agents. Find out, by following imports from `python -m cfo_kernel` and `python -m compiler`, whether the sidecar or the compiler actually imports any of the fourteen modules.

- If a module is only reachable from the old in-process agents, it is bloat. Delete it from `.cfo-v3/kernel` and delete its imports. Do not recreate a skills package to satisfy it.
- If the compiler needs the `tools=` lists on those constructors to generate grants, keep the constructor module and replace `compose_instructions(...)` with a plain string. The compiler reads tools, not prompt text.

Do not bring skill markdown into the kernel.

## Done when

With `PYTHONPATH=/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/kernel` and `.cfo-v3/kernel/.venv/bin/python`:

- `python -m cfo_kernel --help` runs.
- `python -m compiler --out /tmp/cfo-v3-compile` runs and writes a catalog with the same op count as `.cfo-v3/kernel/compile-out/catalog.json`.
- `grep -rn "from skills import" .cfo-v3/kernel --include=*.py` prints nothing.

`engineers/finch/IMPORTS.md` lists each of the fourteen modules, kept or deleted, and why.
