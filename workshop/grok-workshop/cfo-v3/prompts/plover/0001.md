# seq 0001 | tag engineer | id plover | spawn | 2026-09-24T21:46:00Z

Read `/Users/dominikbach/.grok/skills/eng-orch/shared/personas/engineer.md` before you act.

Desk: `/Users/dominikbach/olympus/hackmit/hackmit26/workshop/grok-workshop/cfo-v3`. This is not a bid.

Write scope: `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/cfo/extensions/facade.test.ts`, `/Users/dominikbach/olympus/hackmit/hackmit26/.cfo-v3/harness/intercept.json`, and `engineers/plover/`. Do not edit other extension sources. Do not add `constitution.md`, `SUPERSEDES.md`, or any file on `bloat-eviction.md`.

## Part

`facade.test.ts` has 3 failing tests on `.cfo-v3`. They look for files the old tree had: `harness/intercept.json`, `office/constitution.md`, and similar.

For each failing test, decide:

- The file is a runtime input the extension reads (for example `harness/intercept.json`, which `intercept.ts` loads). Create it in `.cfo-v3` with the same shape as `.cfo-v2/office/computer/harness/intercept.json`, default route to `ctl-pay`.
- The file is on the ban list or only proves the old layout (for example `constitution.md`). Delete that assertion, or change it to assert the v3 path the context module reads (`office/system.md`, `office/bots/<slug>/BOT.md`).

Do not bring banned files back to make a test pass.

## Done when

`engineers/plover/TESTS.md` names each failing test, what you changed, and why. `node --test extensions/facade.test.ts` from `.cfo-v3/cfo` passes, or the remaining failures are listed with the reason.
