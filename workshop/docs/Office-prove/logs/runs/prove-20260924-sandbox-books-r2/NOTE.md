# prove-20260924-sandbox-books-r2

Port 8801. `fakeWorkers` false. Golden stayed `golden-20260920-r1`. Lease file on this Computer lists 16 slugs.

Wake (no Catalog op):

```
profile: erp-invoice
Maximor Demo Corp. Land the NetSuite vendor bill NS-4410 on your desk. Do not match it. Do not close the month. Do not ask a person.
```

- Packet: `workspace/books/packets/NS-4410.json`
- No `workspace/books/erp-invoice/` folder. The first desk, `prove-20260924-sandbox-books`, wrote that invented folder and was not kept as the pass.
- Kernel reads: `invoice_ingestion.tools.get_erp_invoice`, then AP `tools.get_invoice` and `tools.get_case_evidence`.
- No Pi tool result on this desk shows a write blocked for leaving `workspace/books/`. The Bot did not try one.

Fork text that names the packet path: `bots/books/profiles/erp-invoice.md`. Close host writes `workspace/close/packets/<period>.json` and `runs/month_end/`.
