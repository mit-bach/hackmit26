# Fix the behavior. Do not run the month.

Paste this into the debugging chat `a6b2306a-1d5b-4e6a-9bd3-8e9d28b9c786`. Stay on port 8801 and `.cfo-v2/prove-fork/`. Do not POST 8800. Do not edit `.cfo/`, `.cfo-v2/office/`, or Harness `src/` in this pass. Do not record a Demo tape. Do not merge onto golden.

This message replaces the P0–P9 checklist and the “finish P1, then P2” instruction. That loop is why the fork log is a stack of INTENDED and SOFT rows and the skills are still the golden copies.

---

You are the same fork operator. Read these two files before you Wake anyone. They are the defects. They are not a script and they are not a list of sessions.

- `workshop/docs/Office-show/GOLDEN-STATE-SCOPE.md`
- `workshop/docs/Office-show/TOOL-USE-GOLDEN.md`

Also read your own last stretch in this chat. You classified inbox steps, patched two Python files so injection does not mint, and wrote rollup rows. Fork `SKILL.md` and `BOT.md` files are still the golden text. Collect still has no finance call. ctl-books still opens `gates.json` with Pi `read`. World still answers once. That is the work pattern this prompt ends.

## What you stop doing

- Do not run September as one protocol. Do not stack onboard, pay-run, cash, close, and audit on one instance to “cover scope.”
- Do not send a Wake that names a Catalog op, a tool id, or “call tools…”. If the only way a Bot hits the kernel is because you typed the op, the trial is invalid. Delete that sentence and run again.
- Do not treat a classification row as progress. SOFT means you edit the fork prompt that produced it, then retry that one situation on a new small instance.
- Do not paste `ADVERSARIAL-SCENARIOS.md`, holdout ids, or the scheme into a Bot wake. If you have to tell the Bot what was stolen, the check failed.
- Do not clear `$12.40`, mark September CLOSED, load `get_audit_ground_truth`, add a Bot, or hand-edit `grants.json` ops. Skills never grant tools.

## How a trial works

One defect from those two documents. One new fork instance. Two or three Bots, not sixteen. One business situation, written the way an operator would say it: the company, the open item, who owns it, what must not happen. No tool names.

Wait for `turn.end`. Then read the kernel log and the pair thread, not the model’s status line.

Pass only if the behavior showed up on its own:

- A communication trial passes when the two Bots exchange several readable messages on that one item. A verifier may answer once. A worker that replies once and stops fails.
- A tool trial passes when `kernel.log.jsonl` on that instance shows the finance op the situation needs, and the Wake text does not mention it. Pi `bash` and `read` over `data/` or over a host JSON file do not count. A failed lookup (`not_found`, `forbidden`) is a failed trial, not a pass with a note.

If it fails, change the fork material the Bot actually loads: `BOT.md`, profile text, `SKILL.md`, constitution, and kernel code when the tool itself lies or is mislabeled. Change enough that the next trial can come out differently. A one-line comment and another SOFT row is the failure mode of this chat. Then clone a fresh instance and retry that same situation. Do not continue the failed protocol.

Keep each passing instance. Write a short note under `workshop/docs/Office-prove/logs/runs/<id>/` with the Wake you sent (so someone can see it did not name tools), the kernel ops that fired, and the pair-thread length. Leave the failing ones on disk too.

## What “fixed” means

You are not done when the rollup has more checkboxes. You are done when isolated trials, each on its own instance, show these without you coaching the mechanism:

- Outside parties show up as mailbox threads, not one Acme status line. Do this as its own trial, not inside a close.
- A worker thread on one open item keeps going past a single reply. Do this as its own trial.
- An audit trial that is not told about an insider still uses the population reads, not only the loud planted control ids. If the sample is only `PAY-AUD-002`, `VEND-ACME-DUP`, and `JE-AUD-003`, the trial failed.
- A close trial leaves something a person can open, and the reviewer reaches the gate through the finance API. Reading a JSON file the host already wrote is a fail.
- Accrual, prepaid, asset, and balance-sheet situations hit their own tools when that situation is the trial. A pack that says COMPLETE with zero kernel calls is a fail.
- Collect, faced with open receivables, does not stop at an acknowledgement.

Do not schedule these as a campaign. Pick the one that is still false, fix the prompts, prove it on a small desk, then pick the next.

The injection reject you already landed on the fork stays. Do not revert it. Do not copy it to the live tree in this pass.

When a trial passes, say which files you changed and quote the Wake. If the Wake contains a tool id, throw the trial out.
