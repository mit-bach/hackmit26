# Follow-up for the same prove-operator chat

If `prove-operator` is already running: **paste the entire contents of `01-PIVOT-TO-SHOW.md` as the next user message.** That is not a new agent. It is a change of mission.

Do not open a second chat unless the first one is dead. Two chats on 8800 will select different instances and wreck both.

If you never started prove-operator: paste `00-OPERATOR.md` first. When it has a floor that can complete one granted read, paste `01-PIVOT-TO-SHOW.md`. Do not start on golden with no live Pi.
