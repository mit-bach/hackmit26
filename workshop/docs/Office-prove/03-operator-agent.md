# Prove operator agent

One Cursor agent executes `docs/Office-prove/`. Specialist Bots do not read these files.

**Paste-ready brief:** `docs/Office-prove/prompts/00-OPERATOR.md`  
**When time is gone:** paste `docs/Office-prove/prompts/01-PIVOT-TO-SHOW.md` into the **same** chat. That is not a second agent.  
**How to spawn:** `docs/Office-prove/prompts/README.md`

This is not the Floor/AR/AP/Cash/Close repair pack. Those five already landed, including Close. If P0 or a pipe HARD on a missing tool, patch it (small) or stop and say so. Do not assume World, send, Stripe Grants, or lock reads are still missing — read this Computer’s disk.

Spawn **one** prove-operator chat for the first loop. A second chat is legal only after P0 INTENDED, and only for P2 ∥ P3 on two instances. Under time pressure, do not open a `golden-driver` chat. The pivot file is the show driver.

---

## How input is given

Do not paste the corpus into chat. The numbered prompt lists every path. The agent Reads them.

### In Cursor

1. Open one agent chat named `prove-operator`.
2. Working directory: repo root `hackmit26`.
3. **Paste the entire contents of `00-OPERATOR.md` as the first user message.** Not a one-liner. Not “read this path.” The file *is* the prompt.
4. For a second parallel instance (P2 ∥ P3 only, after P0 INTENDED), open a second chat. Give it a different instance name. Do not select the first chat’s instance.

### If you still want to @ files

The shared pack and per-procedure files listed in older drafts are already named inside `00-OPERATOR.md`. Prefer Read-from-disk. Disk on the selected instance wins over `docs/Agentic-update/evidence/live-computer-2026-09-20.md`.

---

## Parallel chats

If you open a second prove chat:

- Different instance id
- Different `logs/runs/<id>/`
- Do not compile on live while the other chat is mid-clone without saying so
- Kernel patches are global. Tell the other chat to restart sidecar after a Kernel HARD patch (restart serve on 8800; selecting the same id is a no-op)

---

## What this agent may edit during HARD

- `.cfo/**/*.py` to stop throws
- Constructor `tools=` then compile
- Client extension generic Grant door
- Harness `src/` generic bus only (Handle unlock, extra `-e`, wipe, spawn routes). No invoice types
- `intercept.json` only if P0 HARD on Operator default
- BOT.md path layout if P0 HARD on cwd

## What this agent may edit only after a procedure (SOFT)

- `SKILL.md` under Kernel and Computer
- BOT.md standing identity and remainder
- Roster `instructions` text (not slug invention)
- `assignments.py` if intersect dropped a required skill

## What this agent must not edit

- Holdout / `expected_results.json` as operational data
- Planted `$12.40` bank line
- `grants.json` ops by hand
- `examples/cfo-floor`
- Demo website hard-coded outcomes
