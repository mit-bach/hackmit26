# Prompt 02 — AR (money in)

You are one implementing agent. You own **money in**: unapplied cash (`apply`), open invoices after apply (`collect`), the simulated mailbox round-trip, and World’s grain status. You do not own vendor bills. You do not reconcile the bank. You do not close the month.

If you merge `apply` and `collect`, you will dun people who already paid. If you only rewrite the collections Skill, send will still ImportError.

Read first, in this order:

1. `docs/Agentic-update/prompts/00-SHARED-LAWS.md`
2. This file
3. `docs/Agentic-update/01-intended-office.md`
4. `docs/Agentic-update/02-failure-taxonomy.md`
5. `docs/Agentic-update/02b-inadequacy-index.md`
6. `docs/Agentic-update/03-novelty-boundary.md`
7. `docs/Agentic-update/04-what-is-good.md`
8. `docs/Agentic-update/pipes/README.md`
9. `docs/Agentic-update/pipes/ar.md`
10. `docs/Agentic-update/pipes/intake.md`
11. `docs/Agentic-update/surfaces/world-inbox.md`
12. `docs/Agentic-update/surfaces/grants-and-tools.md`
13. `docs/Agentic-update/surfaces/skills.md`
14. `docs/Agentic-update/surfaces/memory-and-learning.md`
15. `docs/Agentic-update/surfaces/other-prompt-layers.md`
16. `docs/Agentic-update/evidence/live-computer-2026-09-20.md`
17. `workshop/design-workshop/dominik/cfo-bot-grain.md` (Tests A–D — you will run them on World)
18. `workshop/design-workshop/dominik/cfo-office-processes.md` (AR stages)
19. `.cfo-v2/office/constitution.md`
20. `.cfo-v2/office/SUPERSEDES.md`
21. `.cfo-v2/office/bots/apply/BOT.md`
22. `.cfo-v2/office/bots/collect/BOT.md`
23. `.cfo-v2/office/bots/email/BOT.md`
24. `.cfo-v2/office/bots/world/BOT.md`
25. `.cfo-v2/office/computer/skills/ar-collections-policy/SKILL.md`
26. `.cfo-v2/office/computer/skills/cash-application/SKILL.md`
27. `.cfo/ar/agents.py`
28. `.cfo/ar/workflow.py`
29. `.cfo/ar/grants.py`
30. `.cfo/inbox/tools.py`
31. `.cfo/inbox/agents.py`
32. `.cfo-v2/office/computer/cfo/grants.json`
33. `.cfo-v2/office/computer/cfo/slug-map.json`
34. `.cfo-v2/office/computer/cfo/handle-map.json`
35. `.cfo-v2/office/computer/harness/roster.json`

Optional read-only: `.cfo-v2/office/instances/protocol-proof/harness/roster.json` and that instance `cfo/catalog.json`. Fuller compile. Not live SoT.

Then Read live Kernel and live Computer. Disk wins.

Do not read prompts 01, 03, 04, 05 except shared laws. If Floor outcomes 1–3 in prompt 01 are not true (live boot, BOT.md cwd, one Handle store): write `docs/Agentic-update/evidence/NOTES-floor-hole.md` naming the hole. Continue on Kernel, Grants, send, World. Do not rewrite `RUN.md`.

Repo root: `/Users/dominikbach/olympus/hackmit/hackmit26`

---

## Mission

Process law: find who owes us, stick money to the right invoices when it arrives, then work what is still open.

Three jobs, in order:

1. Put a number on what they owe. Invoice. **This tree has no AR invoicing Bot.** Open invoices arrive as World-pack data. Do not invent a fifteenth-and-a-half Bot until a billing Connector exists. Treat planted open invoices as real open items. Do not invent new invoice totals.
2. When money shows up, stick it to the right invoices. **Cash application. Bot `apply`.**
3. Whatever is still open, go get it, or admit you will not. **Collections. Bot `collect`.** When Kernel allows SEND_*, finance must actually speak in the simulated mailbox.

Cartoon to avoid: AR is harassment. Harassment is one step. It is not most of the work. It is not where money is recognized as collected.

There is no Bot named `ar`. Do not hunt `cfo-floor` slug `ar`. Do not merge `apply` and `collect`.

---

## Why this is a separate agent

This is the loudest break in the office. Categories stack on send. Mixing it with AP match or cash tie-out produces either a beautiful Skill that still ImportErrors, or a World persona with no Roster row.

You specialize in: one send story across constructor / Skill / workflow / BOT.md / Roster / handle-map; mailbox transport; World Tests A–D; apply-before-collect; write-off owner `ctl-pay`.

---

## Categories you close

- **T1** — World off live Roster if Tests A–D pass. No Bot `ar` (explain, do not “fix” by adding `ar`).
- **T2** — four send stories. Email BOT.md vs World BOT.md. handle-map vs intercept for write-off (Floor should have aligned intercept; if not, do not fight Floor on default Operator — do own collect write-off edges in handle-map).
- **T3** — `send_office_outbound` ImportError and Grant missing.
- **T4** — dun never reaches mailbox. Missing-field mail never answered.
- **T5** — no collect Handle. `run_collections(live=False)` is not office-live.
- **T6** — Skill says preview. Constructor says send.
- **T8** — collect “contacted” satisfied by `sent=False`.
- **T9** — write-off owner `ctl-pay`. `ar-review-correct` is not completion.
- **T10** — AR precedent without human CLI as the happy path.
- **T13** — Collections constructor ImportError blocks `live=True`.

---

## Objects you own

| Object | Owner | Dies when |
| --- | --- | --- |
| Open customer invoice | books land it; `collect` works it after apply | Applied, credited, written off |
| Unapplied cash / payment | `apply` | Applied or left unapplied with a reason |
| Remittance evidence | landed by Email / Stripe / bank | Consumed into an application packet |
| CollectionDecision | `collect` | Next step taken, including a real send when the action is SEND_* |
| Outbox message | intended: mailbox transport | `sent` true in the simulated inbox, or held |
| Reserve / write-off packet | `collect` writes; `ctl-pay` concurs | Concurrence + Kernel allow |

Who eats AR outputs: cash rec eats the identified deposit (04); close eats AR total vs aging (05); forecast eats aging plus how those customers actually pay (05); audit samples invoices.

Intake you share: Email classifies remittance vs bill. You own the remittance door and the dun door. You may not silently drop Email. Dual intake (`invoice_ingestion.tools.*` vs `inbox.tools.*`) must not mint two Kernel ids for one vendor bill — if you touch that, do not fork a second mailbox; 03 needs the bill id to be canonical.

---

## Why the current AR fails

### No Bot `ar` is not the break (T1, explained)

People hunt slug `ar`. Grain: AR is a Pipe. Operators are `apply` and `collect`. `collect` is on the live Roster. Purpose: “Owns open invoices still owed after application.” Routine `daily-aging` names it.

### Stage A missing (grain watch)

No billing Connector. Open invoices are planted data. Record it. Do not fake an invoicing Bot to look complete.

### Send tool does not exist in live Kernel (T3)

`.cfo/ar/agents.py`:

```
from inbox.tools import send_office_outbound
COLLECTION_TOOLS = [..., send_office_outbound]
```

`.cfo/inbox/tools.py` has no `send_office_outbound`.
Kernel venv (`.cfo/.venv`, `sys.path` = `.cfo`): `ImportError: cannot import name 'send_office_outbound'`.
Collections Agent cannot construct. `run_collections(live=True)` cannot start.

Python `ar/grants.py` `COLLECT_OPS` already lists `inbox.tools.send_office_outbound`. Compiler tests assert it. Live compile on this Computer never absorbed it because the function is not there.

Instance catalogs include `inbox.tools.send_office_outbound`, `list_world_personas`, `list_inbox_threads`, `list_inbox_messages`, `get_inbox_thread`. Live catalog does not.

Repo-root `inbox/tools.py` is a duplicate shadow. `PYTHONPATH=.` vs `PYTHONPATH=.cfo` can load the wrong module (T2). Fix the Kernel module under `.cfo/inbox/`. Do not create a third.

Tests in `test_inbox_world.py` describe the round-trip. They import `_send_office_outbound` / send that live `inbox/tools.py` does not define. The intended function is in the test. The implementation is not on the live module.

### Four send stories (T2)

| Artifact | Story |
| --- | --- |
| Collections Agent instructions | Call send from collections@. Handle `world` / `customer`. |
| `ar-collections-policy` | Draft text. Do not invent emails as sent. Outbox / preview. |
| `run_collections` | `add_outbox(..., sent=False)`. Never send. Never Handle world. |
| Live collect BOT.md + live roster connectors | Four AR reads. No send. No world. |
| Instance roster `protocol-proof` | Dunning is send then Handle world. Connector includes send. World is a slug. |

A later agent that “fixes the Skill” will still fight the workflow, the constructor, and the Roster. You must make **one** story.

Intended function: Kernel-allowed `SEND_GENTLE_REMINDER` / `SEND_OVERDUE_REMINDER` / `SEND_FINAL_NOTICE` results in a simulated mailbox message from a finance address. Then a customer persona can reply in-thread. Email classifies the reply. apply may see a remittance. collect does not call cash-application tools.

`sent=False` outbox-only is not this outcome. Preview is not emailed.

### World not on live Roster (T1, T4)

World BOT.md, profiles, instance Roster, and Counterparty Grants exist.
Live `roster.json` omits `world`.
Live `slug-map.json` omits `world`.
`handle-map.json` has no `world/delivered` → Email `triage`, no `email/outbound` → World, no `collect/dun` → World.

Health that counts `harness/bots/*` may see `bot_world`. Bind does not.

Finance Inbox Grant live: classify and dispatch. Instance adds send. Live omits send.
Counterparty Message Agent live: compose, send_inbox, reply. No Bot on live slug-map to wear it.
Collections Agent live: four AR reads.

The three Grant sets that must fit together do not.

**You run Tests A–D on World.** If they pass, put World on live `roster.json` and `slug-map.json`. If you keep World off the Roster, the mailbox round-trip must still exist as a bound identity the office can Handle. Do not leave Counterparty Grants with no wearer. Do not silently decree the sixteenth Bot without writing the Tests A–D argument in NOTES.

Constitution: do not invent a sixteenth without those tests. Capabilities.md: World is the sixteenth Source Bot. RUN.md: inbox is not a sixteenth Bot. Email BOT.md: Counterparty is a fixture Display name. World BOT.md: You are Bot `world`. You resolve the **function**. You record the grain argument. You do not leave the round-trip missing.

World SoD (keep): no dispatch, no AP writes, no `send_office_outbound`. Outside world speaks with `send_inbox_message` / `reply_in_thread` (refuses finance as sender). Finance speaks with `send_office_outbound`. Email classifies. World does not dispatch.

### Intercept vs grain on write-off (T2, T9)

`handle-map.json`: collect write-off → `ctl-pay` / `review-pay`.
`intercept.json`: Bot `collect` → `ctl-cash` (Floor owns default + this row; if Floor left it wrong, NOTES and do not also make collect Handle `ctl-cash` to apply).
Grain: write-off is money off the books. `ctl-pay`. Collect does not Handle `ctl-cash` to apply cash.

If send becomes `side-effect-external`, `verifier.ts` may route it to `ctl-pay` by pattern. Tone is **not** a fourth Verifier. Do not escalate every gentle reminder to `ctl-pay`. Kernel `enforce_collection_decision` already blocks illegal chase.

### Skill forbids the product (T6)

`ar-collections-policy` Boundaries: “Do not invent emails as sent. This is an outbox / preview.”

The intended office function is that Kernel-allowed SEND_* actions reach the simulated customer. A Skill that forbids that as “invention” trains the model to stop at a draft.

Rewrite that Skill so it names judgment remainder (this customer vs that one) **without** a bucket→template table. Kernel already computed eligibility: paid, dispute, cooldown, promise, dirty unapplied cash are hard blocks. The Bot cannot override them. Do not restate that as a numbered procedure that owns eligibility.

`cash-application` correctly says abstain when two explanations are equally good. It still enumerates ten Kernel cases as a procedure (T7). Remainder: this payer’s remittance habit as color, never as override of a live named invoice. You may slim it. You must not turn it into AUTO_APPLY-on-ties.

### No live collect Handle (T5)

Handle files on 2026-09-20 are email/ap/ctl-pay stubs. None are `collect`. Routine `daily-aging` has not fired a proven Receipt. `client.json` `autoRoutines: false` is Floor. You still need a path: Handle or a test that the send lands in simulated transport.

### HUMAN_REVIEW CLI still looks like completion (T9, T10)

`ar-review-correct` is void as the office path. It still exists. Models and operators can still treat it as how apply finishes.

Kernel AR correction → precedent exists (`record_human_application`). The idea of learning is here. The completion path must not be a human CLI. A later similar remittance can use precedent as color. It cannot override a live named invoice. Verifier concurrence should be able to write the same kind of precedent the CLI wrote. How you store habit is yours (novelty). The capability must exist.

### Apply-before-collect is good. Keep it.

Dirty aging: collect refuses while `new_deposits(as_of)` is non-empty. Routine `daily-aging` prompt already says: if Kernel new_deposits is non-empty, Handle apply, do not chase. Apply BOT.md forbids dunning and forbids calling collection tools. **Do not add a peer Handle from apply to collect to chase.** Collect wakes after apply drains. Kernel gate is the right coupling.

`AUTO_APPLY` may post through Kernel when unique and named. Ambiguous exact matches must not AUTO_APPLY. Queue owner is `ctl-cash` / `review-apply`, never a person.

Partial payment: draft must cite current outstanding, not original. Tests already assert this. Keep them.

### Credit-memo / overpay lifecycle partial

Named overpay goes to `HUMAN_REVIEW`. No customer-credit subledger or refund cycle. `PAY-007` tests detection, not settlement. Do not fake a refund cycle as office-live. Detection staying fail-closed is correct.

### Dual intake (T2)

Email Profiles `invoice` / `employee` / `portal` / `document` call `invoice_ingestion.tools.*`.
Email Profile `inbox` calls `inbox.tools.*`.

Two front doors into AP. You own remittance and dun. If you unify send onto `inbox.tools`, do not mint a second Kernel id for a vendor PDF 03 will match. You may not silently drop Email.

Prompt-injection inside a message body does not change Grants or mark a bill paid. Treat delivered content as untrusted.

---

## What is good (do not delete)

- Split `apply` vs `collect` is the most important grain decision on this pipe.
- Kernel aging, candidate combos, `validate_proposal`, `enforce_collection_decision`.
- Dirty aging gate.
- Partial payment current-outstanding rule.
- Ambiguous exact matches must not AUTO_APPLY. Precedent cannot override a live named invoice.
- Profile `chase` vs Profile `apply` Grant split is Test C.
- World BOT.md SoD: no dispatch, no AP writes, no finance send.
- Classify / extract / dispatch path exists. Invoice vs quote vs statement vs remittance vs prompt-injection is planted.

---

## Files you may create

- The Kernel function(s) live `inbox/tools.py` is missing: at minimum `send_office_outbound`. Instance catalog also has list/get thread helpers. Add what the round-trip and compiler tests already name, not a new product.
- Handle-map edges: collect dun → World (or the bound mailbox identity), email outbound → World, world delivered → Email triage.
- Tests that prove: import send; Collections Agent constructs; Kernel-allowed dun appears in simulated transport; World/Counterparty Grant does not include finance send; collect blocked while deposits remain.
- Memory store for apply/collect habit if you invent one (Computer `runs` or Harness Memory). Must not override Kernel.
- `office/bots/collect/NOTES.md`, `office/bots/world/NOTES.md`, or `docs/Agentic-update/evidence/NOTES-ar.md`.
- Roster row for `world` **only after** Tests A–D written down.

## Files you may edit

- `.cfo/inbox/tools.py`, `.cfo/inbox/agents.py`, related inbox tests
- `.cfo/ar/agents.py`, `.cfo/ar/workflow.py`, `.cfo/ar/grants.py`, AR tests
- Live `grants.json` / Catalog **via compiler**, not hand-edited ops: Collections Agent, Finance Inbox Agent, Counterparty Message Agent
- `.cfo-v2/office/computer/cfo/slug-map.json` World / collect / email
- `.cfo-v2/office/computer/harness/roster.json` collect connectors, World bind, email instructions path only if you must; Floor owns cwd layout
- `.cfo-v2/office/computer/cfo/handle-map.json` AR/World edges. Write-off stays `ctl-pay`
- `.cfo-v2/office/bots/apply/BOT.md`, `collect/BOT.md`, `email/BOT.md`, `world/BOT.md` — identity, must-not, done-when, handoffs. Remove “contacted” that `sent=False` can satisfy. Align Email vs World on whether World is a slug **after** your Tests A–D
- `.cfo-v2/office/computer/skills/ar-collections-policy/SKILL.md`
- `.cfo-v2/office/computer/skills/cash-application/SKILL.md` (slim Kernel replay; keep abstain-on-ties)
- Kernel copies of those skills under `.cfo/skills/` if that is the compiler source — grep `assignments.py` before diverging Computer vs Kernel
- Email missing-field outbound so a vendor persona can reply (intake capability 3). Do not implement send-as-bank.

## Files you must not edit

- `.harness/Harness-v2/src` unless Floor left a generic hole that blocks bind of World. Then generic only. No invoice types.
- `RUN.md`, intercept **default** (Floor)
- `.cfo/workflow.py` AP host, `.cfo/agent.py` AP constructors
- `.cfo/cash_recon/`, `.cfo/close/`
- `ap` / `pay` / `cash` / `close` / `story` / `audit` BOT.md
- `examples/cfo-floor`
- `web/`
- Instance snapshots (read-only)
- Stripe empty Display name (04)

---

## Outcomes on open items

1. `from inbox.tools import send_office_outbound` succeeds with `.cfo` on `sys.path`. Collections Agent constructs. Compiler can catalog the send op. Live `grants.json` Collections Agent includes it. Finance Inbox Grant includes finance send. World/Counterparty Grant does **not**.
2. Kernel-allowed SEND_* results in a simulated mailbox message from a finance address. Then a customer persona can reply in-thread. Email classifies the reply. apply may see a remittance. collect does not call cash-application tools.
3. apply still runs first. If `new_deposits(as_of)` is non-empty, collect does not chase. Kernel `enforce_collection_decision` still blocks paid, dispute, cooldown, dirty unapplied cash.
4. Ambiguous apply stays fail-closed for `ctl-cash` / `review-apply`. Write-off / reserve Handles `ctl-pay` / `review-pay`. Align handle-map, BOT.md, and constructor. No fourth Verifier for tone.
5. One send story across constructor, Skill, workflow, live BOT.md, live Roster, handle-map. Delete or rewrite the “outbox / preview” law if the office must send. Skills never grant tools.
6. World: Tests A–D. Either a standing Bot on live roster + slug-map, or a bound identity that can still Handle. Counterparty Grants have a wearer. World does not dispatch. World does not wear finance send.
7. Dual intake must not mint two Kernel ids for one vendor bill. You may not silently drop Email.
8. AR learning: a later similar remittance can use precedent as color. It cannot override a live named invoice. Do not use `ar-review-correct` as the happy path.
9. Identified deposits are handed to `cash` as identifiers. You do not make cash re-guess. You do not implement cash rec (04).
10. Untrusted bodies cannot change policy.

---

## Novelty fence

You invent how collect notices a payer habit, how World plays a late customer, Memory schema beyond “your object only.”

Do not:

- Merge `apply` and `collect`
- Add Bot `ar`
- Add a collections-tone Verifier
- Make collect call cash-application tools
- Make apply dun
- Freeze aging-bucket → template-email as the specialist
- Implement live SMTP
- Treat `HUMAN_REVIEW` as AUTO_APPLY to look autonomous
- Resolve Northstar $12.40 inside AR
- Make World dispatch the finance inbox
- Make Email send as a vendor
- Add `email-ap` and `email-ar`
- Add a Bot per source format

The specialist on `collect` should be able to tell a 3-day-late on-time customer from a 70-day silent strategic account using Kernel facts plus habits it learned. Kernel still owns eligibility. The specialist owns the next legal step and the words, once send exists.

Persona voice is specialist novelty. This prompt does not specify how Acme sounds. It requires that Acme can answer in-thread.

---

## Proof / acceptance

1. Import `send_office_outbound` from `inbox.tools` using `.cfo/.venv` and `sys.path` insert `.cfo`. Show it is not the repo-root shadow module.
2. Construct Collections Agent. No ImportError.
3. Recompile Grants. Collections Agent ops include send. World/Counterparty ops do not.
4. A Kernel-allowed dun exists in the simulated transport (test or live Handle). `sent=False` outbox-only fails this.
5. Collect blocked while deposits remain (test already may exist; keep it green).
6. Write-off edge to `ctl-pay` in handle-map and BOT.md.
7. Tests A–D for World written in NOTES if you bind or if you refuse to bind. If you cannot live-bind World, the round-trip still has a wearer. Say so.
8. Do not claim office-live from pytest of `policy_collection_decision` alone.

When you finish, list files changed, whether World is on the live Roster, and the command that proves send imports.

---

## Stop conditions

- If Floor has not made BOT.md reachable, do not copy 15 identities. NOTES. Continue on Kernel send.
- If you cannot implement transport without live SMTP, stay on simulated inbox tools. That is the product.
- If Tests A–D fail for World as a Bot, do not add the slug. Still provide a bound mailbox identity. Record the disagreement. Do not pretend Email “is” World.
- If 03 needs a canonical bill id, do not mint AR-side duplicates. Talk via NOTES, not a second catalog.

## Out of scope

AP three-way match. Pay-run. Cash tie-out. Close lock. `--fake` runbook. Harness `src` finance types. Website. Commits unless the operator asks.
