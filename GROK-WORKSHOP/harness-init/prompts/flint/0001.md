# seq 0001 | tag setup | id flint | spawn | 2026-09-19T18:06:00Z

Read `/Users/dominikbach/.grok/skills/eng-orch/shared/personas/engineer.md` before other work. That file defines who you are and how you work.

You are flint. This window is a spawn. This turn is not a bid. There are no peers. This is a single job. Refine the part. Expect related work later on this id.

Workspace: `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init/engineers/flint/`

Directory you may write:
- `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init/engineers/flint/`
- `/Users/dominikbach/olympus/hackmit/hackmit26/Harness` (the operator named this path as the harness location)

Do not write at desk root. Do not create `Deployment/`. Do not write in `Deployment/`. Do not edit `RUNNING.md`, `KEPT.md`, `CAST.md`, `cast/`, `prompts/`, `AGENT-LOG.md`, or Operator-Workspace. Do not spawn anyone. Do not browse for skills.

Desk: `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init`

There is no operator prompt file. The operator spoke in this parent turn. That speech is the job below.

## Job

Initialize the Foundry Pi harness on this machine.

The operator is on a Hack MIT desk. Later work will be an agentic CFO office. That later work is not this job. This job is the harness that those agents will run on.

The operator named this source as the crystallized product of a prior web Grok session they called bad:

https://github.com/mit-bach/foundry-harness

Start from that repository. Import it. Then initialize it so Pi can actually load it.

The harness tree must live at:

`/Users/dominikbach/olympus/hackmit/hackmit26/Harness`

That directory is empty at the start of this job.

## What this job is not

Do not design the CFO office. Do not invent bots, rooms, or routines for Max Maximir. Do not copy `foundry-output/aegis.json` into a project roster as if that were initialization. Do not open `design-workshop/`. Do not drive a live Pi TUI in the operator's cmux. Do not treat a README paragraph as proof.

## Authority

Read these after the persona, in this order, from the clone once it exists. If the clone does not exist yet, fetch the same files from the GitHub repository, then clone.

1. `README.md` in the foundry-harness tree
2. `SOURCE.md` in the foundry-harness tree
3. `package.json` in the foundry-harness tree
4. `AGENTS.md` in the foundry-harness tree

The operator's named harness path wins if it conflicts with a README clone path. Clone into `/Users/dominikbach/olympus/hackmit/hackmit26/Harness`, not into a new sibling directory.

The latest operator speech wins if it conflicts with the README. The operator wants the tree on disk at the named path, then a real Pi install from that tree.

Parent observations you must re-verify. Do not cite them as proof:

- `pi` is on PATH at `/opt/homebrew/bin/pi` and prints `0.85.1`
- global package `@earendil-works/pi-coding-agent@0.85.1` is installed
- `pi list` printed `No packages installed.`
- `SOURCE.md` asks for Pi ≥ 0.85
- `Harness/` has no files

## What to do

Import the repository into the named harness path. Prefer a git clone of `https://github.com/mit-bach/foundry-harness` into `/Users/dominikbach/olympus/hackmit/hackmit26/Harness` so the tree is a checkout you can inspect.

Then initialize Pi and the Foundry package from that checkout.

README install paths:

```text
npm install -g --ignore-scripts @earendil-works/pi-coding-agent
pi install .
```

or

```text
pi install git:github.com/mit-bach/foundry-harness
```

The operator named the Harness directory as the location of the harness. Use the clone-then-`pi install .` path unless that path is actually blocked. If you also need the git: source on `pi list` to match README wording, say so in the report and show the list line you got.

Confirm with `pi list`. The install is not done until `pi list` shows the Foundry package when you run that command from the directory you would later launch Pi from. Name that directory in the report. Prefer `/Users/dominikbach/olympus/hackmit/hackmit26/Harness` as that launch directory.

If Pi is already present and meets `SOURCE.md`, do not reinstall it as a reflex. Prove the binary, the version, and the package list.

If `pi install .` needs `npm install` in the clone, run that. If peer dependencies or extension files fail, fix only install-blocking defects in the clone. Do not rewrite the floor protocol. Do not redesign extensions. The prior session may be incomplete. Inspect the tree. Say what is actually missing. Repair only what blocks install or load.

Do not `git add -A` in `/Users/dominikbach/olympus/hackmit/hackmit26`. Do not commit the parent Hack MIT repo. A nested git checkout under `Harness/` is allowed.

Keep secrets out of the report. Do not print API keys.

## Prove

Run the commands. Quote the observed output in the report. A claim without a command you ran is unverified.

Prove at least:

1. `Harness/` is a git checkout of `https://github.com/mit-bach/foundry-harness`. Show remote, HEAD, and that `extensions/`, `skills/`, `vendor/`, `foundry-output/`, and `package.json` exist.
2. `which pi` and `pi --version`.
3. `pi list` after install, from the launch directory you named.
4. Whether `node_modules` in the clone is present when `package.json` dependencies require it.
5. What still fails. If an extension cannot load, capture the error. Do not hide it.

If a required command fails, either repair the install-blocking cause and re-run, or stop and write the block. Do not leave `Harness/` empty.

## Product files that must exist when you stop

1. The harness checkout at `/Users/dominikbach/olympus/hackmit/hackmit26/Harness`
2. `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init/engineers/flint/INIT-REPORT.md`
3. `/Users/dominikbach/olympus/hackmit/hackmit26/GROK-WORKSHOP/harness-init/engineers/flint/STOP.md`

`INIT-REPORT.md` is the durable record. It must say what is true on disk, which commands you ran, the observed output, what you repaired, what is broken, and what you did not prove. Use one name for one thing.

`STOP.md` is the stop file. It must say whether the job is done or blocked, which named product files exist, and the next operator-visible fact. If you are blocked, write the block here. Do not wait for a second desk directory.

## Host skill

Who: flint.

Read: `/Users/dominikbach/.grok/skills/ste-writing/SKILL.md`

When: immediately before you write `INIT-REPORT.md` and immediately before you write `STOP.md`. Do not read it once at boot and then draft from memory.

Why: a later parent and a later child must be able to act from those files after this window is gone.

How: follow the skill bands. Use aircraft-strict sentences for steps, command lines to re-run, and refuse lists. Use STE-flavored prose for judgment about what the prior session left incomplete. Leave code fences, paths, command output, and package names intact. Use one name for each object.

## Stop rule

Stop when the three product files exist and the report states, with quoted command output, whether Pi is installed and whether `pi list` shows the Foundry package.

Stop if you are blocked on a missing credential, a network failure you cannot repair, or an upstream defect you cannot fix without redesign. Write that block in `STOP.md` and `INIT-REPORT.md`.

Do not continue into CFO design. Do not continue into a live TUI drive.
